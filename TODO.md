# TODO
## New Features
- add guides for available keys.
- if only one document is open, its section should cover the whole body (instead of just half).

- implement the tool for plain CSV visualization.
- implement the tool for CSV comparison (between two documents).

- have a different color schema when viewing only one JSON document.
    - for "leaf nodes" and "list nonleaf nodes": alternative coloring based on the children's index (i.e. modulo 2).
    - for "dict nonleaf nodes": color differently the key and the value.

- have a different color schema when viewing only one CSV document.
    - follow the schema from the old prismacsv.

- handle more than two documents open at once. Maybe have them in a selectable list, ready to replace either the left or right section of the body.

## Known Bugs
